@model IEnumerable<AssetManagement.Models.DeviceType>

@{
    ViewData["Title"] = "Inactive";
}


<div class="mx-4">
    <h4>Inactive Device Types</h4>
    <a class="btn btn-outline-secondary mb-3" asp-action="Index">Back to List</a>

    <div class="createform"></div>

    <div class="card shadow-sm card-selected">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-datatable">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.DevtypeDescription)
                            </th>
                            <th hidden>
                                @Html.DisplayNameFor(model => model.DevtypeStatus)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.DevtypeCreatedby)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.DateCreated)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.DevtypeUpdateby)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.DateUpdated)
                            </th>
                            <th style="text-align: center!important;">ACTION</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DevtypeDescription)
                                </td>
                                <td hidden>
                                    @Html.DisplayFor(modelItem => item.DevtypeStatus)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DevtypeCreatedby)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DateCreated)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DevtypeUpdateby)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DateUpdated)
                                </td>
                                <td>
                                    <div class="d-flex justify-content-center align-items-center">
                                        <a class="btn btn-success btn-activate" asp-action="Activate" asp-route-id="@item.DevtypeID" data-id="@item.DevtypeID">Activate</a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>



<script>
    document.addEventListener("DOMContentLoaded", function () {
        $('.btn-activate').click(function (e) {
            e.preventDefault();
            var id = $(this).data('id');
            $.ajax({
                url: '@Url.Action("Activate")',
                type: 'POST',
                data: { id: id },
                success: function () {
                    location.reload();
                },
                error: function () {
                    console.log('Error occurred while activating the module.');
                }
            });
        });
    });
</script>
